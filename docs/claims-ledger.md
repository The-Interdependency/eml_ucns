# Claims Ledger for eml_ucns v1.0.0

## Definitions

- `eml(x, y) = exp(x) - log(y)` — implemented.
- `EMLNode` — implemented data structure.

## Implemented Algorithms

- `eml`

## Empirical Results

- **UNRESOLVED** — the repository contains no current executable evidence for
  catalogue-sufficient depth-3 factorization.

## Deprecated

- `eml_tree_to_ucns` — **DEPRECATED** on 2026-08-16 after failing against the
  current UCNS API; the non-leaf constructor was also a placeholder.
- Theorem-N implementation path — **DEPRECATED**; `ucns-theorem-n.md` never
  advanced beyond a placeholder.

## Out of Scope
Carrier widening, general primality.

## hmmm

No current-UCNS adapter, catalogue, theorem statement, or proof witness has
been selected. A successor must start with those exact objects, not inherit
v1 status.
